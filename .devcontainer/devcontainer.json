{
	"name": "Derechos de Estudiantes",
	"dockerComposeFile": "docker-compose.yml",
	"service": "app",
	"workspaceFolder": "/app",
	"forwardPorts": [
		3000
	],
	"postCreateCommand": "cp -n config/database.yml.example config/database.yml && bundle install && bin/rails db:create && bin/rails db:migrate",
	"customizations": {
		"vscode": {
			"extensions": [
				"rebornix.Ruby"
			]
		}
	}
}